#!/bin/bash
str=$2
len=${#str}


if [ $1 -gt $len ]; then
	if [ $3 -lt 0 ]; then
		let "n=$3*-1"
		printf %"$n"s "$str"
	else
		printf %-"$3"s "$str"
	fi
else
	let "less = $1 - 3"
	printf %-."$less"s... "$str"
fi

